const Cancel = () => (
  <div className="cancel-page">
    <h1>❌ Payment Cancelled</h1>
    <p>It looks like your payment was not completed.</p>
    <a href="#pricing">Try again</a>
  </div>
);

export default Cancel;
